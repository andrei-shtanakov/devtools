# Волновой прогон помнит SHA своего мержа — план (prerequisite к #391)

**Spec:** `docs/superpowers/specs/2026-09-24-waves-merge-identity-design.md`
— не согласована на момент написания; Task 0 это фиксирует.

### Task 0: Согласование контракта поля и источника

- [ ] **Step 1** — D1–D4: источник `head` = `merge_commit` заявки
  последней волны; отдельное поле `tree_sha` у `gate-authoritative`;
  исторические `head=None` не чинятся подстановкой.

### Task 1: Запись `head` в волновом пути

- [ ] **Step 1: Failing test** — НАСТОЯЩИЙ путь `candidate → finalize →
  S8`: `head` равен SHA мержа FINALIZE последней заявки и НЕ равен
  `merge_commit` candidate. Разные SHA в фикстуре обязательны — иначе
  тест не отличит происхождение от непустоты.
- [ ] **Step 1a: Реализация записи finalize-SHA** — подтверждённый
  форджей, фиксируется в заявке ДО `complete_request`: после завершения
  заявка терминальна.
- [ ] **Step 2: Failing test** — продвижение базы между фиксацией и
  повторным заходом `head` не меняет.
- [ ] **Step 3: Реализация** — фиксация при переходе последней волны в
  S8, до `_step_s8`; источник — леджер, не форджа.
- [ ] **Step 4: Run + Commit.**

### Task 2: `tree_sha` — дерево прогона отдельно

- [ ] **Step 1: Failing test** — `gate-authoritative` несёт `tree_sha`
  (SHA КОММИТА чекаута, не git-tree), и он отличается от `head`, когда
  база уехала. Плюс: повтор завершённого гейта его НЕ перезаписывает.
- [ ] **Step 2: Реализация + Run + Commit.**

### Task 3: Хвост

- [ ] **Step 1** — исторические леджеры сверены байтами до и после.
- [ ] **Step 2** — #391 снимается с ожидания: проверка `head` до
  создания потомка и тест отказа делаются ТАМ, не здесь (в первой
  редакции плана были запланированы дважды).
