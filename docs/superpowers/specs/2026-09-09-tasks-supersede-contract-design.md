# Переиздание tasks-спеки (supersede) — контракт

**Статус:** draft (спека контракта; код — отдельным заходом после утверждения)
**Предмет:** `governance/task_bridge.py` — санкционированная повторная доставка
tasks-спеки после correction'а вышестоящего бандла.
**Основание:** решение владельца 2026-09-09 — «отдельный заход по supersede
нужен: это повторяемая штатная операция после correction, а не одноразовый
обход. Но сначала короткая спека контракта, затем код».

## 1. Зачем

После correction'а бандла (правка `30-decomposition.md` вышестоящей стадии)
доставленная ранее tasks-спека протухает: она выведена из байтов, которых
больше нет. Штатного пути переиздать её нет — мост видит завершённый op
`tasks-deliver`, находит прежний PR и принимает доставку как состоявшуюся.
Ручная чистка леджера недопустима (решение владельца): леджер — журнал фактов,
а не рабочая поверхность оператора.

Боевой случай: воркстрим `verify-first-file-scope-group-targets-20260908`
(spec-runner#403) — семь verify-задач переразмечены correction'ом, tasks-спека
помечена `stale`, переиздать её нечем.

«Байты, которых больше нет» — это **содержание** апстрима, а не подпись под
ним: переиздание нужно тогда и только тогда, когда изменился вход, из которого
выведена спека. Кто именно подписал одобрение — отдельный факт, и на вопрос «а
надо ли переиздавать» он не отвечает. Отсюда две идентичности §I2.

## 2. Границы

- Переиздание — **операторская команда с явным флагом**, не автоматика.
- Мержи PR-ов переизданием **не выполняются** (ни бандла, ни tasks) — решение
  владельца: «никаких автоматических мержей».
- Смена состава DAG (`--legacy-bundle`) supersede не касается: переиздание
  идёт тем же активным DAG, что и исходная доставка.

## 3. Инварианты (перечень владельца, дословно расшифрован)

Правка 2026-09-09 (по итогам реализации, PR #165): §I2 и §I5 исправлены
**вместе** — §I2 разводит две идентичности и задаёт порядок вычисления, §I5
сравнивает signature-free `content_anchor`; §I7 уточнён до **поузлового**
provenance. Дефект был в определении, а не в коде: один anchor отвечал сразу на
два вопроса и потому делал §I5 и §I7 одновременно невыполнимыми (§7).

Правка того же дня (по итогам ревью реализации): §I7 **отвязан от файла
терминального узла**. Поиск correction-PR идёт по последним коммитам всех
файлов активного DAG, собственные delivery-PR воркстрима исключаются явно, а
требование «PR обязан менять анкер» снято. Поузловое правило, оставленное с
поиском по одному узлу, вырождалось обратно в bundle-wide — и вырождалось
молча (§7).

Правка того же дня (issue devtools#172, решение владельца): §I7 дополнен
**таблицей переходов по статусу узла**. Поузловое правило было записано для
одного состояния входа — узла, уже находящегося в `approved`, — и для
`draft`/`stale` молча не выполнялось, хотя именно эти статусы correction и
оставляет за собой (§7).

### I1. Новая ветка от свежего base

Переиздание создаёт **новую** ветку доставки от только что подтянутого
`base_ref` и не переиспользует ветку прежней доставки. Причина (боевые круги
3–5 PR devtools#161): `ensure_branch` на существующую ветку откатывает дерево к
её содержимому, и спека рендерится из устаревшего бандла.

Имя: `spec/<ws-id>-tasks-v<N>`, где `N` — порядковый номер переиздания
(первое → `v2`). `N` выводится из истории доставок в леджере (см. I4), а не из
существующих веток: ветка могла быть удалена после мержа.

### I2. Две идентичности: артефактный anchor и `content_anchor`

Переиздание задаёт два **разных** вопроса, и одна величина ответить на оба не
может:

- «идентичны ли доставленные байты» — вопрос об **артефакте**: те ли байты ушли
  в PR, тот ли коммит лежит в ветке, сходится ли реконсиляция;
- «менялся ли содержательно **апстрим**» — вопрос о входе: есть ли вообще что
  переиздавать.

Поэтому идентичностей две, у каждой своё имя и свой вопрос.

**Артефактный anchor** (`prospective_anchor` в намерении, `anchor` в
завершённой ревизии) — blob-хеш **терминального узла активного DAG после
штампа** (`stamp_bundle_approved`), точный, побайтовый. Именно эти байты уходят
в PR и после его мержа лежат в base. Пред-штамповый anchor непригоден:
сравнение с base после мержа никогда не совпало бы, и гейт стал бы fail-open.
Артефактный anchor служит намерению, сверке фактического штампа с проспективным
(ниже в этом же §I2) и реконсиляции ревизии (§I3) — и только им.

На вопрос «менялся ли апстрим» артефактный anchor не отвечает **по
построению**: подпись (`approved_by`/`approved_at`) лежит ВНУТРИ сравниваемых
байтов, а подпись переиздания по §I7 берётся у correction-PR, то есть заведомо
отличается от подписи предыдущей доставки. Равенства не бывает никогда — и
каждый `--supersede` плодил бы `v3`, `v4`, … даже без единой правки апстрима
(наблюдалось на боевых данных, PR #165).

**`content_anchor`** — канонический хеш активного DAG, из которого **исключена
подпись**. Signature-free обязано быть не только представление самого узла: его
`upstream_hashes` в каноническом представлении пересчитываются из таких же
канонических, signature-free представлений upstream-узлов. Иначе подпись
протекает каскадом через пины, и `content_anchor` снова отвечает на вопрос о
подписи, а не об апстриме. **Только `content_anchor` используется в §I5.**

Канонизация обязана удовлетворять двум требованиям, и они обязательны:

1. результат не зависит **ни от одного** значения подписи ни в одном узле DAG —
   ни прямо, ни через пины;
2. процедура одна и та же в момент записи `content_anchor` в леджер и в момент
   сравнения с ним — иначе равный вход даст разные хеши, и §I5 снова не
   сработает никогда.

Из (2) следует: смена процедуры канонизации делает старые записи
несравнимыми — они проходят compatibility-случай §6, а не сравниваются по
новому правилу задним числом.

**Определение канонизации.** Это часть контракта, а не деталь реализации:

- **представление узла** — текст его файла, у которого из frontmatter удалены
  `approved_by`/`approved_at`, а `upstream_hashes` заменены на **канонические**
  блобы upstream-узлов, то есть `blob_sha1` их же signature-free
  представлений, вычисленных этим же правилом рекурсивно (обход DAG
  топологический, поэтому к моменту узла его upstream'ы уже посчитаны). Второй
  пункт не украшение: реальный пин считается с байтов upstream-файла ВМЕСТЕ с
  подписью, и без пересчёта смена подписи одного узла каскадом меняла бы пины
  всех, кто его пинует, — подпись протекла бы в «содержание» ровно там, где
  §I5 обязан её не видеть;
- **хеш DAG — манифест по ВСЕМ узлам**, а не блоб терминального: `blob_sha1`
  строк вида «`<node-id> <канонический блоб>`», по строке на узел в порядке
  DAG. Терминальный узел пинует лишь своё транзитивное замыкание, и узел вне
  замыкания в хеш бы не вошёл, а «хеш активного DAG» обязан покрывать активный
  DAG целиком. Сегодня замыкание полное во всех четырёх вариантах `_dag_for` —
  но это свойство **данных**, а не определения, и опираться на него контракт не
  может;
- **`status` и `version` НЕ вырезаются.** Их меняет только переход
  draft → approved, а обе стороны сверки §I5 считаются по проспективно
  проштампованному дереву (см. ниже), где переход уже совершён, — значит они
  совпадают сами. Нормализовать их не просто лишнее, а вредно: хеш стал бы слеп
  к реальному откату узла в `draft`;
- **представление берётся с проспективно проштампованного состояния, с
  подписью-заглушкой.** Без штампа доставка v1, чей base ещё `draft`, записала
  бы хеш доштамповых байтов, а переиздание считало бы его по уже
  проштампованному base — §I5 расходился бы на пустом месте. Заглушка законна
  ровно потому, что канонизация подпись вырезает: результат от её значения не
  зависит. **Именно это и делает новый порядок §I2 возможным** — §I5 стоит ДО
  сетевого разрешения §I7, потому что `content_anchor` провенанса не требует
  вовсе. Отсюда же следует, что канонический штамп идёт **без** `signed_nodes`
  и поузловую политику §I7 не применяет: на шаге 3 состав подписываемых узлов
  ещё не вычислен и вычислен быть не может. Противоречия с §I7 тут нет —
  `content_anchor` сравнивается только сам с собой, а требование (2) выполнено
  тем, что обе стороны сверки считаются одной и той же нейтральной процедурой.

**Порядок вычисления.** Обе величины считаются до любого delivery-эффекта
(ветка, коммит, push, PR, запись в леджер), но **после обязательной
синхронизации чистого base**. Git-чекаут здесь допустим и необходим — он не
delivery-эффект; бесследность (§I5) относится к `run.json`, а не к рабочему
дереву.

1. `checkout_and_pull(base_ref)` — синхронизация чистого base (дерево
   переключается, это ожидаемо; dirty-гард обязан пройти ДО неё);
2. реконсиляция ревизий (§I3) и сверка состава активного DAG (§I8): обе идут по
   фактам леджера и git, подпись им не нужна;
3. `content_anchor` активного DAG — signature-free, см. выше;
4. **§I5**: `content_anchor` равен записанному у последней завершённой доставки
   → успешный бесследный no-op, выход; леджер не пишется вовсе;
5. только если апстрим изменился — разрешается provenance correction-PR
   (§3.I7): его `mergedBy`/`mergedAt` входят в штамп, а пересечение его
   состава файлов с активным DAG (`signed_nodes`) определяет, какие узлы этот
   штамп получат; судьбу остальных узлов решает их статус — таблица поузловой
   политики §I7;
6. в памяти строится **prospective stamp**: те же преобразования, что делает
   `stamp_bundle_approved`, но результат никуда не пишется. «Те же» — включая
   поузловую политику §I7 целиком: и клетки, сохраняющие прежнюю подпись, и
   клетки fail-closed. Иначе сверка «проспективный штамп = фактический»
   перестала бы быть тождеством, а отказ по `draft` вне `signed_nodes`
   наступал бы уже после коммита и push;
7. `prospective_anchor = blob_sha1(<проштампованный в памяти терминальный
   узел>)`;
8. доставка.

Порядок именно такой не ради вкуса. Прежняя редакция разрешала provenance
вторым шагом («без него проспективный штамп не вычислим») и порождала
чикен-эгг: чтобы вычислить anchor, нужна подпись; чтобы понять, нужна ли
доставка вообще, нужен anchor. В боевом случае он ещё и самоподтверждался:
прежняя редакция §I7 искала provenance по последнему коммиту, изменившему файл
анкера, а после доставки это штамп-коммит **нашего же** прошлого tasks-PR —
переиздание принимало собственную предыдущую доставку за correction.

Обе половины этого дефекта закрыты порознь и обе нужны. **Порядок**: на вопрос
§I5 отвечает `content_anchor`, которому подпись не нужна, и бесследный no-op
наступает **до** попытки разрешить provenance вовсе. **Сам §I7**: поиск больше
не привязан к файлу анкера, а собственные delivery-PR воркстрима исключаются
явно — иначе при изменившемся апстриме (когда §I5 пропускает дальше по праву)
самоподтверждение вернулось бы ровно тем же путём.

В леджер пишутся **обе** величины: `content_anchor` — чтобы следующий запуск
мог ответить на вопрос §I5; артефактный `anchor` — чтобы реконсиляция могла
ответить на вопрос §I3. Одна другую не заменяет.

Фактический штамп на диск ложится позже, уже внутри доставки, и обязан совпасть
с проспективным — расхождение фатально (значит вход изменился между проверкой и
эффектом).

### I3. Реконсиляция ревизии: явные переходы, идентичность важнее имени ветки

Совпадения имени ветки недостаточно — ветка могла остаться от другого захода.
Ревизия реконсилируется по паре «состояние op × состояние PR», и в каждом
случае сверяется **идентичность** (что именно сверяется — ниже).

| Состояние ревизии | PR по её ветке | base | Действие |
|---|---|---|---|
| `completed` | OPEN | — | вернуть существующий PR; `v<N+1>` НЕ создаётся |
| `completed` | MERGED | — | доставка состоялась; переиздание решается только по I5 |
| `started` | OPEN | `base_sha` совпал | сверить идентичность → продолжить (докоммитить/допушить) |
| `started` | OPEN | `base_sha` сдвинулся | **fail-closed** (§I4-конфликт, см. ниже) |
| `started` | MERGED | — | сверить идентичность → завершить op этим PR |
| `started` | нет PR | `base_sha` совпал | восстановление по шагам §I3.1 (коммит/push могли частично состояться) |
| `started` | нет PR | `base_sha` сдвинулся | ревизия помечается `abandoned`, заводится следующий `N` |
| любое | CLOSED-unmerged | — | **fail-closed**: ветка отклонена человеком |
| любое | идентичность не сошлась | — | **fail-closed** |

**Что сверяет идентичность.** Здесь и ниже речь о вопросе «те ли байты», то
есть об **артефактном** anchor'е (§I2); `content_anchor` в реконсиляции не
участвует. «Пути + пин анкера» недостаточно: они не определяют содержимое спеки
(тот же артефактный anchor даёт разные байты при другом `generated_at`).
Намерение (§I4) фиксирует детерминирующие поля, и сверяются именно они:

- `expected_generated_at` — штамп генерации, зафиксированный при старте
  ревизии (рендер обязан использовать его, а не текущее время: иначе
  повторный заход даст другие байты при том же входе);
- `tasks_blob` — ожидаемый blob-хеш файла `spec/<ws-id>-tasks.md`, каким его
  даёт детерминированный рендер по зафиксированным полям;
- `head_sha` — записывается **durable сразу после создания коммита и до
  `push`**: иначе падение между коммитом и push оставляет ревизию без
  единственного факта, по которому её можно опознать в удалённой ветке.

**§I3.1. Коммит и запись `head_sha` не атомарны** (замечание владельца):
падение между ними оставляет коммит в дереве при `head_sha: null`. Поэтому
восстановление ревизии `started` без PR раскладывается по состояниям, а не
«продолжает с шага создания PR»:

| `head_sha` | Факт в git | Действие |
|---|---|---|
| `null` | подходящего коммита нет | создать коммит, durable записать SHA, затем push |
| `null` | есть коммит (local или remote) с ожидаемыми `base_sha`-родителем, tree и `tasks_blob` | **принять его**: durable записать его SHA, затем push |
| `null` | есть коммит, но `base_sha`/tree/`tasks_blob` не те | fail-closed: чужой коммит в ветке |
| записан | remote-ветки нет | push **именно этого SHA** (не пересоздавать коммит) |
| записан | remote head совпал | продолжить с создания PR |
| записан | remote head отличается | fail-closed: ветку двигали снаружи |

«Подходящий коммит» опознаётся тремя фактами намерения одновременно: родитель
равен `base_sha`, дерево содержит спеку с `tasks_blob`, и сам файл спеки несёт
`expected_generated_at`. Совпадения одного факта недостаточно.

Сверка: `head` PR-а равен записанному `head_sha`, а доставленный в нём файл
спеки — записанному `tasks_blob`. Расхождение любого — fail-closed: под тем же
именем ветки может лежать чужая работа.

**Конфликт I3×I4 разрешён явно** (замечание владельца): для `started` + OPEN PR
+ сдвинувшийся `base_sha` **нельзя** ни продолжать старый PR (он выведен из
другого base), ни молча заводить следующую ревизию (останутся два открытых PR,
а закрытие старого потом попадёт под вечный `CLOSED-unmerged` → fail-closed).
Правило: **fail-closed с операторским разрешением** — переиздание отказывает и
называет открытый PR; оператор закрывает/абандонит его явно
(`--abandon-revision <N>`, который переводит ревизию в терминальный
`abandoned` и фиксирует причину), после чего заводится следующая ревизия.
Терминальные статусы ревизии: `completed` и `abandoned` — и **только они**.
Статуса `superseded` нет: перевод уже терминальной записи в другое состояние
был бы мутацией журнала (замечание владельца). Вместо этого **новая** ревизия
несёт поле `supersedes: <N>` — связь направлена вперёд, старые записи
неприкосновенны, а причина `abandoned` сохраняется навсегда.
`CLOSED-unmerged` у ревизии в статусе `abandoned` — ожидаемое состояние, а не
отказ.

### I4. Завершённый op не перезаписывается

`op_complete("tasks-deliver", …)` прежней доставки **остаётся неизменным**.
Переиздание добавляет отдельную запись истории — ревизию:

```
# v1, неприкосновенна
ops["tasks-deliver"]    = {status: completed, pr: 392,
                           anchor: <blob>, content_anchor: <canon>}
# переиздание
ops["tasks-deliver-v2"] = {status: completed, pr: 4xx,
                           anchor: <blob'>, content_anchor: <canon'>}
```

Завершённая ревизия несёт **обе** идентичности §I2: `anchor` — артефактный,
для реконсиляции; `content_anchor` — signature-free, для §I5 следующего
переиздания.

Форма ключа ревизии — `tasks-deliver-v<N>`, тот же `N`, что в имени ветки (I1).
Отсюда же выводится `N` для следующего переиздания: максимальный существующий
плюс один. Провалившееся переиздание оставляет свою ревизию в `started` и не
трогает предыдущие — повторный заход возобновляет её (write-ahead, как везде в
леджере).

**`op_start` фиксирует намерение целиком.** Без этого после падения или сдвига
base нельзя отличить «возобновляется тот же supersede» от «начинается другой».
Записываются:

```
ops["tasks-deliver-v2"] = {
  status:                "started",           # → completed | abandoned | superseded
  revision:              2,
  branch:                "spec/<ws-id>-tasks-v2",
  base_sha:              "<sha base_ref на момент старта>",
  prospective_anchor:    "<артефактный blob, §I2>",
  content_anchor:        "<канонический signature-free хеш DAG, §I2>",
  approval_pr:           <номер correction-PR, §I7>,
  signed_nodes:          ["<node-id>", …],   # PR.files ∩ активный DAG, §I7
  tasks_version:         <версия, которую получит спека, §I6>,
  dag:                   ["00-charter.md", …],   # активный DAG
  dag_source:            "previous_delivery" | "derived_from_spec" | "unavailable",  # §I8
  supersedes:            <N-1> | null,   # связь вперёд; старые записи не мутируются
  expected_generated_at: "<штамп генерации, §I3>",
  tasks_blob:            "<ожидаемый blob спеки, §I3>",
  head_sha:              null   # заполняется durable сразу после коммита, ДО push
}
```

Возобновление сверяет намерение с фактом. `base_sha` сдвинулся (сверка идёт в
шаге 2 порядка §I2, без подписи) или `prospective_anchor` пересчитался иначе
(эта сверка возможна только после разрешения provenance, то есть уже на самой
доставке) — это уже **другое** переиздание: если
у старой ревизии нет открытого PR, она помечается `abandoned` и заводится
следующий `N`; если открытый PR есть — fail-closed до операторского
`--abandon-revision` (§I3). Молчаливое продолжение под тем же ключом
запрещено.

**Повтор детерминирован намерением, а не фактами форджи.** Всё, что определяет
байты доставки, ревизия несёт сама: `expected_generated_at`, `tasks_version`,
`branch`, `dag` — и `signed_nodes`, состав подписываемых узлов §I7. Множество
вычисляется **один раз**, на шаге 5 §I7, как пересечение состава файлов
correction-PR с файлами активного DAG, и сразу ложится в намерение durable. На
пути возобновления состав берётся **из намерения** и не пересчитывается из
фактов correction-PR: изменившийся ответ форджи (PR дополнили коммитом, состав
файлов поехал) дал бы другой штамп, а с ним другой артефактный anchor — и
доставка упёрлась бы в гард §I2 «фактический штамп разошёлся с проспективным»
на собственном повторе. Намерения, записанные до поузлового §I7, `signed_nodes`
не несут: для них состав берётся из фактов correction-PR.

Статусы узлов, от которых зависит вторая половина штампа (таблица §I7), ревизия
отдельным полем не несёт и нести не должна: они целиком определены содержимым
base, а base зафиксирован в намерении как `base_sha` и сверяется при
возобновлении. Сдвинулся base — это уже другое переиздание (см. выше), и
статусы пересчитываются вместе со всем остальным.

### I5. Равный `content_anchor` → успешный бесследный no-op

Сравнивается **`content_anchor`** (§I2), а не артефактный anchor. Причина
прямая: §I5 спрашивает про **апстрим**, а `approved_by`/`approved_at` — это
метаданные одобрения, не апстрим. Артефактный anchor их содержит, поэтому по
нему «апстрим не менялся» недостижимо в принципе (§I2), и §I5 никогда бы не
сработал.

Если `content_anchor` активного DAG **равен** `content_anchor`'у последней
завершённой доставки, переиздание не выполняется: апстрим не менялся,
переиздавать нечего. Это **успех, а не отказ**: RC 0, сообщение «апстрим не
менялся — переиздание не требуется». Не создаются ни ветка, ни PR; леджер не
пишется вовсе — ни `started`, ни новая ревизия. Provenance correction-PR к
этому моменту ещё не разрешался и не разрешается вовсе (§I2, шаги 4–5).

Свойство «бесследности» проверяемо: снимок `run.json` до и после вызова
побайтово совпадает.

### I6. Монотонная версия tasks-спеки

`version:` в frontmatter переизданной спеки строго больше версии предыдущей
доставленной (читается из `spec/<ws-id>-tasks.md` в свежем base). Сброс в `1`
недопустим: версия — счётчик поколений документа, а не счётчик вызовов
рендера.

### I7. Штамп correction'а — по correction-PR, поузлово

`approved_by`/`approved_at` берутся из фактов **того PR, который доставил
correction** (mergedBy/mergedAt correction-PR), а не исходного бандл-PR. Иначе
штамп утверждает, что текущие байты одобрил человек, который одобрял другую
версию.

**Provenance поузловой, а не bundle-wide.** Подпись correction-PR получают
**только те узлы активного DAG, которые этот PR действительно менял**. Узлы,
которых он не касался, сохраняют исходный provenance: их байты одобрял тот, кто
их одобрял, и переписывать эту подпись — то же самое искажение, только в другую
сторону. Сохранить прежнюю подпись можно, однако, не в любом состоянии узла:
границы задаёт таблица «Поузловая политика по статусу узла» ниже, и она — часть
правила, а не уточнение к нему.

**Механическая перепиновка подпись не меняет.** Если у downstream-узла
обновились `upstream_hashes` (пин пересчитан, потому что изменились байты
upstream'а), то сам по себе этот факт **не** делает correction-PR автором
одобрения этого узла: содержательно узел не менялся, изменилась только его
привязка к изменившемуся входу. Без этого уточнения поузловое правило
схлопывается обратно в bundle-wide — перепиновка каскадом доходит до
терминального узла, и подпись correction-PR расползлась бы по всему DAG.

Именно этот узел и встречается в base со статусом `stale`: `stale` ставит не
мост, а сам correction, и означает он «изменился upstream», а не «изменилось
содержимое узла». Так что «перепиновка — не approve» и «`stale` сохраняет
подпись» — одно и то же утверждение, увиденное с двух сторон: из механики
штампа и из статуса, который эта механика оставляет в base.

Алгоритм ниже даёт это свойство самим механизмом, а не отдельной проверкой:
`signed_nodes` выводится из состава файлов **correction-PR**, а перепиновка
downstream выполняется штампом самого переиздания — уже после того, как подпись
определена, и в состав correction-PR она не попадает.

**Поузловая политика по статусу узла.** Всё сказанное выше описывает узел,
который уже `approved`. Этого мало: `draft` и `stale` — не краевой случай, а
**штатный след correction'а**. Коррекция бандла меняет байты upstream-узла, и
downstream приезжает ровно в эти статусы; переиздание встречает их на основном
пути, а не на обочине. Поэтому контракт обязан отвечать на вопрос для
**каждой** клетки «статус узла × принадлежность `signed_nodes`», а не для
одной из них.

Таблица описывает штамп **переиздания** — тот, у которого `signed_nodes`
вычислено (§I2, шаг 5). У обычной первой доставки `signed_nodes` нет вовсе:
там штамп идёт по факту мержа бандл-PR и применяется ко всем узлам DAG,
поэтому строки «вне `signed_nodes`» к ней неприменимы.

| Статус узла в base | В `signed_nodes` | Действие |
|---|---|---|
| `draft` | да | → `approved`, подпись — `mergedBy`/`mergedAt` correction-PR |
| `stale` | да | → `approved`, подпись — `mergedBy`/`mergedAt` correction-PR |
| `approved` | да | подпись correction-PR; перезапись условная — совпала, узел не трогается |
| `stale` | нет | → `approved`, перепиновать, `approved_by`/`approved_at` — **прежние** |
| `approved` | нет | статус и подпись не трогать (перепиновка — механическая, см. выше) |
| `draft` | нет | **fail-closed** |
| любой | нет, и прежней подписи нет | **fail-closed** |

Прежняя подпись считается отсутствующей и тогда, когда поле есть, но пусто:
шаблон бандла заводит `approved_by: ""` до всякого approve, и пустая строка —
это «не подписано», а не подпись.

**Почему `stale` вне `signed_nodes` сохраняет прежнюю подпись.** `stale`
означает, что изменился **upstream**, а не собственное содержимое узла.
Одобряли же именно собственные байты; они не менялись, значит прежнее
одобрение остаётся в силе, и меняются только статус и механические пины —
ровно те величины, что перепиновкой и заводятся. Приписать такому узлу подпись
correction-PR значило бы утверждать, что человек, мерживший correction,
одобрил байты, которых тот PR не касался. Это тот же дефект, ради которого §I7
и стал поузловым, только проникающий через статус, а не через состав файлов.

**Почему `draft` вне `signed_nodes` — fail-closed, а не «сохранить подпись».**
Довод про `stale` здесь не проходит: `draft` не утверждает, что содержимое
узла прежнее. Содержимое могло изменить **другой** correction-PR, тот, который
мы не разрешали и чьи факты нам недоступны, — и доказать обратное нечем.
Сохранение старой подписи в этом состоянии **недоказуемо**, а недоказуемую
подпись переиздание не ставит. Правильный ответ оператора — не ослабить
правило, а предъявить провенанс: назвать `--approval-pr` тот correction,
состав файлов которого этот узел покрывает (тогда узел попадает в
`signed_nodes` и получает честную подпись), либо доставить правку этого узла
отдельным PR и переиздать после его мержа.

**Почему отсутствующая прежняя подпись — тоже fail-closed.** Правило для узла
вне `signed_nodes` — «сохранить прежнее одобрение»; сохранять нечего, если
одобрения не было. Альтернатива — выпустить узел `approved` с пустой подписью,
то есть утверждение «одобрено» без одобрившего. Это хуже отказа: пустая
подпись неотличима от потерянной, и аудит по леджеру её не поймает.

Оба fail-closed'а наступают на **проспективном** штампе (§I2, шаги 6–7), то
есть до ветки, коммита, push и PR: переиздание отказывает, ничего не создав.
Достижимы они только после того, как §I5 пропустил дальше, — если апстрим не
менялся, переиздание завершается бесследным no-op'ом независимо от статусов
узлов.

**Открытый вопрос (контрактом не закрыт).** Растёт ли `version` узла при
переходе `stale` → `approved` **вне** `signed_nodes`, решение владельца не
фиксирует: переход в `approved` совершается, но approve-события у этого узла
нет. Сформулированное правило («инкремент привязан к переходу draft →
approved») на эту клетку однозначного ответа не даёт, и контракт его здесь
намеренно не выдумывает. Речь о `version` узла бандла; к монотонной версии
tasks-спеки (§I6) вопрос отношения не имеет.

**Инвариант provenance — общий для обоих путей.** Автоматический поиск и явный
`--approval-pr` приходят к одному и тому же номеру разными дорогами, но
принимается номер по одним и тем же четырём условиям:

- PR merged в `base_ref`;
- PR менял хотя бы один узел активного DAG;
- PR **не является собственной tasks-доставкой этого воркстрима**;
- `signed_nodes` = `PR.files ∩ активный DAG`.

Ни одно из них флагом не отменяется. Инвариант — про **истинность подписи**, а
не про удобство поиска: собственная предыдущая доставка не становится
correction-PR оттого, что её назвали вслух, и принять её означало бы
санкционировать заведомо ложную подпись — штамп утверждал бы, что байты одобрил
человек, который на самом деле мержил нашу же прошлую доставку.

**Алгоритм provenance (исполним, без догадок):**

1. Взять последний коммит, изменивший **каждый** файл активного DAG в свежем
   `base_ref`: `git log -1 --format=%H -- <bundle_dir>/<file>` по каждому узлу.
   Поиск **не** стартует от файла терминального узла: штатный correction правит
   upstream-узел и терминального может не касаться вовсе (перепиновку
   downstream мы сами объявили механической и отложили до штампа переиздания).
   Ни одного коммита ни по одному файлу — отказ: correction не найден.
2. Связать полученные коммиты с PR-ами (`gh api .../commits/<sha>/pulls`),
   оставить только `MERGED` и нацеленные в `base_ref`.
3. Исключить **собственные delivery-PR этого воркстрима** — это третий пункт
   общего инварианта, применённый на стороне поиска (на явном пути он же
   проверяется шагом 5). Опознание двухуровневое:
   - **первично — по номерам из леджера текущего прогона**: `pr` каждой записи
     `tasks-deliver` и `tasks-deliver-v<N>`;
   - **дополнительно — по своим веткам, разрешённым в номера**: для доставок,
     чей номер ещё не записан, берётся ветка **намерения** (`branch` ревизии,
     для легаси-v1 — `spec/<ws-id>-tasks`) и резолвится в номер PR запросом по
     ветке (`find_pr`, любое состояние). Ветку отдаёт **только запись в статусе
     `started`**: намерение пишется write-ahead, ДО создания PR, поэтому у
     `started` номера может ещё не быть; у `completed` он есть по построению, а
     `abandoned` у форджи не спрашивается вовсе — терминальную запись не
     переспрашивают (§I3).

   Обе линии сравнивают **свои собственные факты**, а не чужие имена. Номер в
   леджере — факт, записанный **нами самими** в момент доставки: он опознаёт
   нашу доставку и после переименования ветки, и после её удаления, и
   независимо от того, кто её мержил. Вторая линия достраивает ровно ту дыру,
   которую оставляет write-ahead, — и делает это, разрешая **нашу** ветку в
   номер, а не сверяя имя ветки кандидата с шаблоном. Сверка по шаблону имени
   была бы **слишком слабой идентичностью** (имя может совпасть у чужой ветки
   и разойтись у нашей) и вдобавок опиралась бы на имя ветки кандидата —
   поля, которого слой ops не отдаёт ни на одном пути: проверка была бы мёртвой
   в проде.

   **Принятый остаток.** Так не опознаётся delivery-PR **чужого прогона** того
   же воркстрима: его номера в нашем леджере нет и его ветки в наших
   намерениях нет. Такой PR остаётся вторым кандидатом и уводит поиск в
   fail-closed шага 4 — отказ с подсказкой `--approval-pr <n>`, а не подпись
   мимо человека. Это осознанный выбор, а не недоделка: неопределённость
   разрешается отказом, и «чинить» её молчаливым пропуском кандидата нельзя.
4. Требуется **ровно один** кандидат — он и есть correction-PR. **Ноль**
   (правка вне PR, прямой коммит, squash без связи — либо единственным
   кандидатом была собственная доставка и её снял шаг 3) или **больше
   одного** → fail-closed: подписи взять неоткуда, гадать нельзя; оператор
   называет PR явно (`--approval-pr <n>`).
5. Проверить кандидата — **одинаково на обоих путях**, автоматическом и
   `--approval-pr`:
   - PR вмержен и нацелен в `base_ref`, `mergedBy`/`mergedAt` непусты;
   - PR **не является собственной tasks-доставкой этого воркстрима**:
     опознание — прежде всего по номерам из леджера (первичная линия шага 3);
     namespace собственных веток работает дополнительным предохранителем на
     стороне поиска, где его достраивает write-ahead-дыра. На явном пути
     сетевой резолв веток не выполняется: оператор называет номер, который
     видел, а первичная линия — леджер — работает на обоих путях одинаково;
   - `signed_nodes` = состав файлов PR ∩ файлы активного DAG (в границах
     `<bundle_dir>`, приведённые к node-id);
   - `signed_nodes` **непуст**: PR, не тронувший ни одного узла активного DAG,
     correction'ом этого DAG не является — подписи взять неоткуда.

   Проверка общая намеренно: обещание §I7 не должно зависеть от того, каким
   путём пришёл номер.
6. `approved_by = mergedBy.login`, `approved_at = mergedAt`; подпись получают
   **только** узлы из `signed_nodes` (правило поузлового provenance выше).
   Остальные узлы активного DAG проходят по таблице «Поузловая политика по
   статусу узла»: `approved` и `stale` сохраняют свою подпись (`stale`
   вдобавок переводится в `approved`), `draft` и узел без прежней подписи —
   fail-closed.

Требования «PR обязан менять файл терминального узла» в контракте **нет** — ни
на автоматическом пути, ни на явном. Оно и было тем условием, которое
схлопывало поузловое правило обратно в bundle-wide, причём молча:
самоподтверждалось штампом нашей же прошлой доставки (§7). Его место заняли
проверки шага 5, общие для обоих путей.

Состав узлов едет **вместе с подписью**, одним результатом: иначе вызывающая
сторона решала бы «кому подписывать» отдельно от «чья подпись», а это два
ответа на один вопрос. В намерение ревизии он ложится durable как
`signed_nodes` (§I4) сразу, и повтор доставки берёт его оттуда, а не
пересчитывает из фактов форджи.

Алгоритм исполняется в шаге 5 порядка §I2 — то есть только после того, как §I5
уже сказал, что апстрим менялся.

Инвариант, ради которого всё это: **подпись не может быть взята у PR, который
correction'а не доставлял, и собственная прошлая доставка за correction принята
быть не может — ни молча, ни по явному указанию оператора.** На автоматическом
пути его держит шаг 3, на явном — шаг 5; оба обеспечивают его механизмом, а не
рассуждением.

**Fallback `--approval-pr <n>`** — для случаев, когда автоматика даёт ноль или
несколько кандидатов, оператор называет PR явно. Флаг заменяет **обнаружение
кандидата и разрешение неоднозначности** — шаги 1–2 (сбор коммитов по узлам,
связывание с PR) и требование единственности шага 4, — но **не отменяет
проверку истинности провенанса**: шаг 5 выполняется целиком, всеми четырьмя
условиями общего инварианта. Явно названный PR обязан быть merged, нацелен в
`base_ref`, не быть собственной доставкой этого воркстрима и менять хотя бы
один узел активного DAG — иначе отказ.

Флаг говорит «вот номер», а не «вот подпись». Собственная предыдущая доставка
не становится correction'ом оттого, что её назвали вслух: принять её означало
бы санкционировать заведомо ложную подпись — ровно тот дефект, ради которого
шаг 3 и появился, только пропущенный через операторскую дверь.

**Если другого кандидата нет — это честный fail-closed**, а не потерянный
операторский выход. Состояние, в котором единственный вмерженный в `base_ref`
PR по узлам DAG — наша же прошлая доставка, означает, что источника
достоверной подписи действительно не существует: correction'а никто не
доставлял. Правильный ответ здесь — отказ с диагностикой (и, если правка
апстрима реальна, доставить её PR-ом), а не подпись, взятая у мержа нашей
собственной доставки. Возвращать оператору выход через ложную подпись нельзя.

### I8. «Тот же активный DAG» — сверяемое утверждение, не декларация

§2 требует, чтобы переиздание шло тем же активным DAG, что исходная доставка.
Запись `dag` в новой ревизии фиксирует **выбор**, но ничего не доказывает —
поэтому DAG обязан быть **выведен из предыдущей доставки и сверен**:

- если предыдущая ревизия несёт `dag` — новая обязана совпасть с ним;
  расхождение — fail-closed («переиздание меняет состав DAG — это не
  переиздание, а другая доставка»);
- если предыдущая ревизия `dag` не несёт (легаси-v1), он выводится из
  **доставленной спеки в base** и состава каталога бандла: результат обязан
  **точно совпасть с одним из** `_dag_for(None)`, `_dag_for(3)`, `_dag_for(4)`,
  `_dag_for(5)`. Именно точное совпадение, а не «префикс текущего DAG»:
  `--legacy-bundle=5` — отдельный вариант со своим терминальным узлом
  (decomposition пинует только design), а не срез полного DAG. Совпал ровно
  один вариант → он и есть активный DAG предыдущей доставки, дальше сверка как
  обычно; совпало ноль или больше одного → вывод не удался;
- вывод не удался (спеки нет, frontmatter не разобрать) → compatibility-случай
  (§6) с явной записью `dag_source: "unavailable"` в ревизии: в журнале
  зафиксировано, что состав не сверялся, а не что он совпал.

### I9. Никаких автоматических мержей

Переиздание открывает PR и останавливается. Мерж — человеческий (tasks-спека
исполнима: её approve и есть санкция на исполнение).

## 4. Наблюдаемое поведение (для приёмки)

| Вход | Наблюдаемое | RC |
|---|---|---|
| `--supersede`, `content_anchor` изменился, прежняя доставка вмержена | новая ветка `-v<N>`, новый PR, ревизия `tasks-deliver-v<N>` с обоими anchor'ами, версия спеки +1 | 0 |
| `--supersede`, `content_anchor` равен записанному у последней доставки | «апстрим не менялся — переиздание не требуется»; `run.json` побайтово прежний; ветки и PR нет; provenance correction-PR **не разрешался** | **0** |
| `--supersede` дважды подряд без правок апстрима | второй запуск — тот же no-op; `v3`, `v4`, … не заводятся (боевой дефект PR #165: артефактный anchor не совпадал никогда из-за подписи) | **0** |
| `--supersede`, ревизия `completed`, её PR OPEN | возвращается существующий PR; `v<N+1>` не создаётся | 0 |
| `--supersede`, ревизия `started`, PR OPEN, идентичность сошлась | доставка продолжается в той же ветке | 0 |
| `--supersede`, ревизия `started`, PR MERGED, идентичность сошлась | op завершается этим PR | 0 |
| `--supersede`, PR CLOSED-unmerged | fail-closed: ветка отклонена человеком | ≠0 |
| `--supersede`, идентичность не сошлась (`head`/артефакт чужие) | fail-closed с указанием расхождения | ≠0 |
| `--supersede`, `started`-ревизия, base сдвинулся, PR у неё НЕТ | старая ревизия → `abandoned`, заводится следующий `N` | 0 |
| `--supersede`, `started`-ревизия, base сдвинулся, PR OPEN | fail-closed с именем PR; требуется `--abandon-revision <N>` | ≠0 |
| `--abandon-revision <N>` | ревизия → `abandoned` с причиной; следующий запуск заводит новый `N` | 0 |
| `--supersede`, `head`/`tasks_blob` PR-а не совпал с намерением | fail-closed: чужая работа под тем же именем ветки | ≠0 |
| `--supersede`, состав DAG отличается от предыдущей доставки | fail-closed: «это не переиздание, а другая доставка» | ≠0 |
| `--supersede`, correction-PR: 0 или >1 кандидатов, без `--approval-pr` | отказ: «подписи взять неоткуда» (проверка достижима только когда `content_anchor` изменился) | ≠0 |
| `--supersede`, correction правил только upstream-узел, терминального не трогал | correction-PR найден по коммиту этого узла (поиск идёт по всем файлам DAG); `signed_nodes` = {этот узел}; остальные узлы, включая терминальный, штампуются по таблице §I7 — переиздание выполняется, если каждый из них `approved`/`stale` с прежней подписью | 0 |
| `--supersede`, единственный кандидат — собственный delivery-PR воркстрима (штамп прошлой доставки) | исключён шагом 3 §I7 (номер из леджера; для ещё не записанного номера — своя ветка намерения, разрешённая в номер); кандидатов ноль → отказ с диагностикой. Ни молчаливого принятия своей доставки за correction, ни принятия по явному `--approval-pr` (тот же PR отвергнет шаг 5) | ≠0 |
| `--supersede`, среди кандидатов delivery-PR **чужого прогона** того же воркстрима | не опознаётся (его номера и ветки в нашем леджере нет) и остаётся вторым кандидатом → fail-closed шага 4 с подсказкой `--approval-pr <n>`. Принятый остаток: неопределённость разрешается отказом, а не догадкой | ≠0 |
| `--supersede`, два настоящих внешних кандидата после исключения своих | fail-closed: «подписи взять неоткуда»; оператор называет PR явно | ≠0 |
| `--supersede --approval-pr <n>`, PR не merged / не в `base_ref` | отказ той же проверки (шаг 5 §I7) | ≠0 |
| `--supersede --approval-pr <n>`, PR вмержен в `base_ref`, но не менял ни одного файла активного DAG | отказ: `signed_nodes` пуст — correction'ом этого DAG PR не является | ≠0 |
| `--supersede --approval-pr <n>`, назван собственный delivery-PR воркстрима | отказ той же проверки (шаг 5 §I7): собственная доставка не становится correction'ом оттого, что её назвали явно. Флаг заменяет обнаружение кандидата, а не проверку истинности провенанса; отсутствие другого кандидата — честный fail-closed | ≠0 |
| `--supersede`, correction правил часть узлов DAG | подпись correction-PR получают только эти узлы; нетронутые — по таблице поузловой политики §I7; downstream с одной лишь перепиновкой `upstream_hashes` подпись не меняет (перепиновка идёт штампом переиздания и в состав correction-PR не входит) | 0 |
| `--supersede`, узел вне `signed_nodes` лежит `stale` и несёт прежнюю подпись | узел переводится в `approved` и перепиновывается, `approved_by`/`approved_at` остаются прежними; переиздание выполняется | 0 |
| `--supersede`, узел вне `signed_nodes` лежит `draft` | fail-closed: сохранение прежней подписи недоказуемо; в диагностике — назвать `--approval-pr`, покрывающий этот узел, либо доставить его правку отдельным PR. Отказ наступает на проспективном штампе: ветки, коммита и PR нет | ≠0 |
| `--supersede`, у узла вне `signed_nodes` прежней подписи нет (поле пусто или отсутствует) | fail-closed: сохранять нечего, а `approved` без подписавшего контракт не выпускает | ≠0 |
| `--supersede`, узлы вне `signed_nodes` лежат `draft`, но апстрим не менялся | бесследный no-op §I5: проверки статусов недостижимы, provenance не разрешался | **0** |
| `--supersede`, возобновление ревизии, состав файлов correction-PR с тех пор изменился | штамп идёт по `signed_nodes` из намерения — байты те же, гард §I2 не срабатывает | 0 |
| `--supersede`, у последней доставки не записан `content_anchor` (легаси, в т.ч. запись только с `anchor`) | одноразовый compatibility-случай: ревизия несёт `comparison: unavailable`, переиздание выполняется | 0 |
| без флага, op завершён | сегодняшнее поведение: адоптирует прежний PR | 0 |
| без флага, op отсутствует | сегодняшнее поведение: обычная доставка | 0 |

## 5. Вне объёма

- Автоматическое обнаружение необходимости переиздания (кто-то должен решить,
  что correction состоялся).
- Ретроспективная починка леджеров, где anchor'ы не записывались (см. вместо
  неё compatibility-случай §6).
- Каскад на downstream-артефакты spec-runner (`status: stale` ставится
  correction'ом бандла, не мостом).

## 6. Compatibility-случай: доставка без записанного `content_anchor`

Сравнение §I5 идёт по `content_anchor` (§I2), поэтому под этот случай
попадают **обе** категории старых записей:

- доставки, выполненные до введения anchor'а вовсе, — в леджере нет ничего;
- доставки, записавшие только артефактный `anchor`, — сравнивать по нему §I5
  нельзя.

Для обеих сравнение невозможно **по построению**, и одного warning'а
недостаточно: неизвестное должно остаться неизвестным в самом журнале, а не
раствориться в логе.

Правило — одноразовый compatibility-случай:

- переиздание выполняется (иначе такие воркстримы неремонтируемы);
- ревизия несёт явные поля `comparison: "unavailable"` (сравнивать не с чем) и,
  если состав DAG вывести не удалось, `dag_source: "unavailable"` (§I8) —
  записи фиксируют, ЧТО именно не сверялось;
- в выводе оператору сказано то же самое: сверка не производилась, потому что
  предыдущая доставка `content_anchor` не записала;
- случай одноразовый: новая ревизия `content_anchor` записывает, поэтому
  следующее переиздание того же воркстрима уже сверяется нормально.

`comparison: "unavailable"` — это не «сверка прошла», а «сверки не было»:
любой последующий аудит отличает одно от другого по самому леджеру.

**Переосмысливать поле `anchor` задним числом нельзя.** Записанный `anchor` —
артефактный blob-хеш (§I2), и он остаётся им во всех старых записях. Ни
подставлять его на место `content_anchor` в §I5, ни трактовать «anchor не
совпал» как «апстрим менялся» недопустимо: это ровно тот дефект, который
контракт и чинит. Отсутствие `content_anchor` — это `comparison: unavailable`,
а не повод достроить его из того, что записано.

## 7. Уроки, которые контракт закрывает

Пять кругов ревью devtools#161 показали: локального фикса порядка операций
недостаточно, потому что переиздание конфликтовало с моделью «одна ветка / один
PR / штамп внутри PR». Контракт разводит их явно: переиздание — новое
поколение доставки со своей веткой, своим PR и своей записью в леджере, а не
повторный проход по существующей.

**Одна величина на два вопроса рано или поздно столкнёт два инварианта — и
столкнёт их в бою, а не в тестах.** Первая редакция §I2 определяла ОДИН anchor
и молча поручала ему два разных вопроса: «идентичны ли доставленные байты» и
«менялся ли апстрим». Пока величина одна, §I5 («равный anchor → no-op») и §I7
(«подпись — от correction-PR») выполнимы только поодиночке: подпись лежит
внутри сравниваемых байтов, поэтому либо равенства не бывает никогда и
переиздание плодит `v3`, `v4`, … без единой правки апстрима, либо артефакт
утверждает, что скорректированные байты одобрил человек, одобрявший другую
версию. Противоречие было не между инвариантами — их столкнуло определение.

Тесты этого не поймали и поймать не могли: в них подписи совпадали, а в бою —
нет. Отсюда общее правило для будущих контрактов: если одну величину
спрашивают о двух разных вещах, это не экономия, а отложенный дефект —
разводить идентичности надо в определении, до того как на них встанут
инварианты.

**Поузловое правило, оставленное с привязкой поиска к одному узлу,
вырождается обратно в bundle-wide — и вырождается МОЛЧА.** §I7 уже был сделан
поузловым, но шаг поиска остался привязан к файлу терминального узла. На
реалистичном входе — correction правит только `10-requirements.md`, а
`30-decomposition.md` не трогает, потому что перепиновку downstream мы сами
объявили механической и отложили до штампа переиздания — последним коммитом по
файлу анкера оказывается штамп-коммит **нашего же** прошлого tasks-PR. Он
вмержен в `base_ref`, кандидат единственный, и проверку «PR менял анкер» он
проходит — своим же штампом. Итог: RC 0, ни отказа, ни предупреждения, а
подписи ВСЕХ узлов переписываются данными человека, мержившего предыдущую
доставку; состав нашего tasks-PR покрывает весь DAG, поэтому поузловой
алгоритм даёт ровно bundle-wide результат. Явный путь при этом отвергал
настоящий correction-PR (тот анкера не менял) — воркстрим оказывался
неремонтируем ни одним из двух путей.

Правило на будущее: проверка «PR трогал X», где X — артефакт, который наш же
инструмент штатно трогает, самоподтверждается и защиты не даёт. Собственные
доставки надо исключать **явно и по фактам, записанным нами самими** (номер PR
в леджере прогона; там, где номера ещё нет, — своя ветка намерения, разрешённая
в номер), а не выводить из того, кто какие файлы менял, и не сверять имя чужой
ветки с шаблоном: шаблон опознаёт не нашу доставку, а совпадение строки.

**Правило, записанное для одного состояния входа, для остальных состояний того
же входа молча не выполняется.** §I7 был сделан поузловым и проверен на узле,
который уже `approved`: там «узел вне `signed_nodes` сохраняет свою подпись»
держалось. Ветка, переводящая узел из `draft`/`stale` в `approved`, — это тот
же вход в другом состоянии, и она ставила подпись **безусловно**,
`signed_nodes` не глядя. Отказа не было, предупреждения не было: узел просто
выходил из штампа подписанным человеком, который его файла не касался. На
доставке spec-runner#408 (`signed_nodes` = behaviour-spec + decomposition) так
подписались `20-design.md` и `25-acceptance.md` — лежавшие `stale`, с байтами
из более раннего #403, — от имени мержившего correction-PR #407.

Отдельный урок внутри того же: **`draft`/`stale` были не краевым случаем, а
основным путём.** Correction бандла приводит downstream ровно в эти статусы —
то есть переиздание встречает их каждый раз, а не в редкой конфигурации.
Правило писалось по состоянию, которое было под рукой (base уже проштампован
предыдущей доставкой, всё `approved`), и штатный след собственной операции
остался за рамками. Отсюда общее требование к формулировкам инвариантов:
правило, адресованное узлу/записи/файлу, обязано перечислить **все**
состояния, в которых эта сущность бывает, и назвать действие для каждого — а
состояния, которые порождает наш же штатный сценарий, проверять первыми.
