# Контрольный прогон хвоста S7 (@id:s7-control-run)

**Дата:** 2026-09-21. **Прогон:** `review-pr-unreachable-base-coverage-20260921-3f8af3`.
**Цель:** `devtools` (свой репо), клон `out/live/devtools-s7` @ `1563a0e`.
**Предмет:** реальный открытый хвост `@id:review-scope-unreachable-base-coverage`
— непокрытый `die 2` при недостижимой базе в `review-pr.sh`, не филлер.
**Вход:** `--brief` (не `--need`) — рукописный customer-бриф, прошедший
вендоренный gate_check с 0 findings.
**Харнесс авторинга:** `claude` / `claude-opus-5`, один проход, без
автоматических повторов; первый отказ — остановка и разбор.

Предмет проверки — §9.2 спеки need-stage
(`docs/superpowers/specs/2026-09-15-need-stage-design.md`). Исполнение задач
в критерий не входит.

## Круг 1 — остановка на S4

Все шесть узлов авторинга — `exit 0` с первого захода; бандл (7 файлов,
1576 строк) закоммичен локально. Гейт S4 отказал пятью находками одного
класса:

```
error GC-DT-CONTRACT: DT-01: delivers DEL-09 ссылается на charter#CON-01 —
  узел 'charter' в бандле отсутствует
  (известны: acceptance, behaviour-spec, decomposition, design, requirements)
```

Остальные 42 ссылки `sources` разрешились (acceptance 18, behaviour-spec 9,
design 8, requirements 7). Сломался прибор, не авторинг: гейт перечислял
узлы собственным кортежем, и `charter` в него не попал, хотя
`00-charter.md` лежал в бандле. Классы `CON-*`, `M-*`, `OUT-*`, `RK-*`
существуют ТОЛЬКО в charter — значит у результата поставки, происходящего от
ограничения или метрики, не было в бандле ни одного законного адреса.

Починено **PR #313** (`81d0bae`): состав индекса выводится из объявленного
состава бандла (`bundle_dag.node_filenames()` над активным DAG), второго
перечня узлов не остаётся; кратность определения доезжает до проверки ссылки
(`node_id_counts`, ссылка на дважды определённый id отвергается как
неоднозначная). Контракт уточнён — §3b.2 спеки decomposition-node.

### Миграция бандла

Коммит `3dae57f` на ветке прогона. Только оформление: 14 заголовков
`#### <ID>` вставлены НАД существующими пунктами, потеряно 0 исходных строк,
все добавленные непустые строки — заголовки (сверено против закоммиченного
оригинала `966f151`). Тринадцать пунктов, у которых id уже стоял в начале,
повышены механически; `M-01` — редакционное решение владельца о месте
определения, текст дословно. Шесть id, упомянутых только внутри прозы
(`G-01/02`, `P-01/02`, `J-01/02`), в определения НЕ повышались.

`upstream_hashes` пересчитаны каскадом в топологическом порядке `BUNDLE_DAG`
— 8 пинов в 5 узлах, замена текстовая по строке пина. Первая попытка шла
через `split_frontmatter`/`join_frontmatter` и переписала
`traces_to: [charter]` в блочный стиль, сняв кавычки с хэшей; откачено как
правка авторского текста сверх задачи.

## Круг 2 — §9.2 по пунктам

| критерий §9.2 | факт |
|---|---|
| раннер САМ дошёл до `waiting_human_merge` | да, без вмешательства после зелёного S4 |
| мерж бандл-PR через `make human-merge` | #314, `merged_by=andrei-shtanakov`, голова пинована `--expect-head` |
| `resume` подтвердил факт и перевёл на S8 | `merge: completed (merged=True)` |
| S8 `exit=0`, НЕ `merged_unverified` | `gate-authoritative: completed (exit=0)`, `status: completed` |
| заведён draft tasks-PR | #328 (draft) |
| получена approved tasks-спека | `status: approved`, `version: 2`, `approved_by: andrei-shtanakov`, `validation: pass`, `traces_to: [decomposition]`, пин на вмерженный узел |

Нормализация frontmatter после approve — PR #329.

Путь §I12: бандл-PR #314 → узлы #316→#317, #318→#319, #320→#321, #322→#323,
#324→#325, #326→#327 → tasks-PR #328 → conform #329.

## Негативный контроль — СОГЛАСОВАННОЕ ОТКЛОНЕНИЕ

Штатный путь (`waiting_human_merge` → мерж человеком → реконсиляция) пройден
живьём. Обход — мерж вне раннера из `stopped_review`/`stopped_author`/
`stopped_gate` — живьём НЕ воспроизводился. Вместо него три регрессии на
зафиксированном SHA `81d0bae`:

- `test_resume_from_stopped_review_pr_merged_out_of_band_runs_s8`
- `test_resume_from_stopped_author_pr_merged_out_of_band_runs_s8`
- `test_resume_from_stopped_gate_pr_merged_out_of_band_runs_s8`

3 passed. **Это отклонение, а не выполнение**: живой обход остаётся
непроверенным, и ссылаться на прогон как на доказательство §9.2 ЦЕЛИКОМ
нельзя.

## Человеческие акты — девять вместо восьми

1 мерж бандл-PR + 6 мержей candidate-PR §I12 + 1 approve tasks-спеки +
**1 потерянный**: candidate #315 смержен верно, но финализирующую команду
запускал агент, у которого `AUTHORIZED_APPROVER_ACCOUNTS` в окружении не
было. Контур увидел пустой allowlist, подписи не признал, заявка
`approve-1-0-1` — `invalidated`. Операторская ошибка, не цена процедуры.

## Находки конвейера

| # | наблюдаемый дефект | статус |
|---|---|---|
| 1 | charter не адресуем в `delivers.sources` | закрыто #313 |
| 2 | агентский мерж finalize отклоняет форджа (6/6 узлов, код 4) | devtools#277, подтверждено повторно |
| 3 | `AUTHORIZED_APPROVER_ACCOUNTS` не переживает границу процессов | новый пункт |
| 4 | политика подписи приходит из окружения проверяющего | новый пункт |
| 5 | зелёный гейт не стирает `gate-findings.txt` прошлого круга | новый пункт |
| 6 | tasks-PR кодовый из-за `evidence/*.jsonl` | новый пункт |
| 7 | штамп одобрения переформатирует авторский frontmatter | новый пункт |
| 8 | Copilot пришёл на #317 вопреки правилу «OFF по умолчанию» | новый пункт |

Подробности — в соответствующих пунктах `TODO.md`.

## Цена

Платного: **один проход авторинга** шести узлов (claude-opus-5, все `exit=0`
с первого раза) и **один круг терминального ревью** на PR #313 (approve, 0
находок). Ревью бандл-PR и всех шести конвертов подписи — бесплатные
scope-аттестации `kind=prose-only`: модель не вызывалась, бюджет не
списывался. Денежного потолка инструмент не даёт: `ops._author_argv`
собирает вызов без `--max-budget-usd`.
