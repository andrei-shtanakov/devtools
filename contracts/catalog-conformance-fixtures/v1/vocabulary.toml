# Машиночитаемый словарь enum'ов ADR-ECO-003 (devtools#51) — потребители
# ВЕНДОРЯТ его вместе с набором вместо рукописных копий прозы.
#
# ПРОИЗВОДНЫЙ от ADR-ECO-003 (prograph-vault/authored/decisions/
# 2026-07-01-adr-eco-003-agent-catalog.md; в ADR словарь живёт инлайн-
# комментариями в примерах Plane 1/2), не независимый источник: расхождение
# с ADR — дефект ЭТОГО файла, править здесь. Owner-side у devtools второй
# копии нет: референс-валидатор check-catalog-fixtures.py читает этот файл.
#
# Аддитивность: добавление значения — изменение v1 (новая фикстура/кейс по
# необходимости, regen manifest); удаление или переименование значения
# меняет семантику существующих кейсов — это v2.

version = 1

model_status = ["active", "deprecated", "retired"]
harness_kind = ["cli", "api-baseline", "local"]
